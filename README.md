# Kate Ker — Portfolio

Static GitHub Pages site for [kate-ker.github.io](https://kate-ker.github.io/).

## Local preview

Open `index.html` in a browser, or from this folder:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
